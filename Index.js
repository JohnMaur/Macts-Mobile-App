import React from 'react';
import { AppRegistry } from 'react-native';
import Main from './Main'; // Assuming your main wrapper component is named Main

const MyApp = () => <Main />;

AppRegistry.registerComponent('MyApp', () => MyApp);
